name: monkeytype-stats
on:
  schedule:
    - cron: "0 */6 * * *"
  workflow_dispatch:

jobs:
  update:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: monkeytype-hub/monkeytype-readme@master
        with:
          username: 'clemente_abarzua'
          github-token: ${{ secrets.GITHUB_TOKEN }}
